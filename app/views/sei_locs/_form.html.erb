<%= form_for(@sei_loc) do |f| %>
  <% if @sei_loc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sei_loc.errors.count, "error") %> prohibited this sei_loc from being saved:</h2>

      <ul>
      <% @sei_loc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
